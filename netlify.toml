[build]
  command = "pnpm build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  # Static export (`output: 'export'`) — do not inject the Next.js runtime plugin.
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
